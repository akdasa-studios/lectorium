name: Mobile

on:
  push:
    # paths:
    #   - "modules/apps/mobile/**"
    #   - ".github/workflows/mobile-build.yml"
  
env:
  REGISTRY: ghcr.io
  ORGANIZATION: akdasa-studios

jobs:
  build:
    name: 👷‍♂️ Build
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v3
        with:
          submodules: true

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'oracle'
          java-version: '21'

      - name: 🛠️ Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: 'modules/package-lock.json'

      - name: 📦 Install Dependencies
        working-directory: modules/apps/mobile
        run: npm ci

      - name: 🏗️ Build Ionic App
        working-directory: modules/apps/mobile
        run: npx cap build android

      # - name: 📤 Upload Build Artifacts
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: ionic-build
      #     path: modules/apps/mobile/www
      #     retention-days: 7
      
        
      # - name: Check Vulnerabilities
      #   uses: aquasecurity/trivy-action@v0.3.0
      #   with:
      #     image-ref: ${{ env.REGISTRY }}/${{ env.ORGANIZATION }}/lectorium-mobile:staging
      #     format: 'json'
      #     output: 'trivy-report.json'
      #     ignore-unfixed: true
      #     severity: 'HIGH,CRITICAL'
      #     exit-code: 1
      #     ignore-policy: '.github/actions/trivy-policy.json'
