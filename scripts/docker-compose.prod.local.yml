name: lectorium

services:
  database:
    environment:
      - COUCHDB_USER=lectorium
      - COUCHDB_PASSWORD=lectorium

  signed-url-generator:
    environment:
      - S3_ACCESS_KEY_ID=
      - S3_SECRET_ACCESS_KEY=
      - S3_BUCKET_NAME=
      - S3_ENDPOINT_URL=
      - REGION_NAME=
